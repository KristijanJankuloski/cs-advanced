<Window x:Class="taxi_workshop.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:taxi_workshop.Desktop"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Login" Height="250" Width="300" ResizeMode="NoResize" Icon="/taxi.png">
    <Grid Margin="20, 20" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Width="200">
            <Label>Username:</Label>
            <TextBox x:Name="txtUsername" Padding="2" />
        </StackPanel>
        <StackPanel Grid.Row="1" Width="200">
            <Label>Password:</Label>
            <PasswordBox x:Name="txtPassword" Padding="2" />
        </StackPanel>
        <StackPanel Grid.Row="2" Width="200">
            <Button x:Name="btnLogin" Width="100" Click="btnLogin_Click">Log in</Button>
        </StackPanel>
    </Grid>
</Window>
